
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pr-451
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/TUM-Dev/navigatum.git
    targetRevision: www.nav.tum.de
    path: deployment/k3s
    helm:
      version: v3
      values: |
        namespace: pr-451
        tag: pr-451
        url: pr-451.nav.tum.sexy
        server:
            MEILI_MASTER_KEY: MDNhOWJlY2QyN2E1MjA0NDFiOWZmNzBiZjljZDQ0NmQ4NTdkNmM0M2ZiMjFiNmQzMjMK
            # GITHUB_TOKEN is intentionally omitted, as this file is templated and then added to git in the deployments branch
            # JWT_KEY is omitted, because adding it would only make sense in conjunction with GITHUB_TOKEN
            # MEILI_MASTER_KEY being added is not a security risk, since it is a hidden service and even if exposed is heavily restricted by other network policies
  destination:
    server: https://kubernetes.default.svc
    namespace: pr-451
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
      - PrunePropagationPolicy=foreground
      - CreateNamespace=true
    retry:
      limit: 2
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m0s

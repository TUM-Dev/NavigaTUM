apiVersion: apps/v1
kind: Deployment
metadata:
    name: navigatum-data
    labels:
        app: navigatum
    namespace: navigatum
spec:
    replicas: 1
    selector:
        matchLabels:
            app: navigatum
    template:
        metadata:
            labels:
                app: navigatum
        spec:
            containers:
                - name: navigatum-data
                  image: ghcr.io/TUM-Dev/navigatum-data:main
                  imagePullPolicy: Always
                  ports:
                      - containerPort: 8000
                        name: navigatum-cdn